# Meerkit monitor plugin template

Copy this directory, change the module path and manifest identity, then implement the SDK `Module` interface. Use `desp` for the plugin summary and `url` for its source or release page. Create an importable archive with the root `scripts/package-plugins.sh --plugin`; Meerkit never imports a raw plugin executable.
